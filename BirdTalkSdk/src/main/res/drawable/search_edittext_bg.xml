<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 背景颜色 -->
    <solid android:color="#FFFFFF" />

    <!-- 边框样式 -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0"
        android:dashWidth="0dp"
        android:dashGap="0dp" />

    <!-- 圆角半径（控制圆弧大小，值越大角越圆） -->
    <corners android:radius="24dp" />  <!-- 24dp适合大多数搜索框，可根据需要调整 -->

    <!-- 内边距（让文字不紧贴边框） -->
    <padding
        android:bottom="8dp"
        android:left="12dp"
        android:right="12dp"
        android:top="8dp" />

</shape>

